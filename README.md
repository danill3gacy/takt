# Такт — предматчевый отчёт по сопернику

[![checks](https://github.com/danill3gacy/takt/actions/workflows/ci.yml/badge.svg)](https://github.com/danill3gacy/takt/actions/workflows/ci.yml)
[![Python 3.11+](https://img.shields.io/badge/python-3.11%2B-blue)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/license-MIT-green)](LICENSE)
[![Code style: typed](https://img.shields.io/badge/code-typed-informational)](takt/model.py)

Надстройка над данными лиги для тренерского штаба. Забирает у видеоаналитика
механику — выгрузку, сведение, разметку, нарезку — и к утру отдаёт собранный
отчёт по сопернику, где каждый тезис кликается в эпизоды.

Не «ИИ вместо аналитика». Аналитик перестаёт работать руками и начинает
работать головой: система готовит материал, он проверяет, спорит, добавляет
то, что видит глазом, и несёт тренеру уже своё.

```
python -m takt.cli report --team "Auckland FC" --out out/report.html
```

## Ключевые цифры

| Показатель | Значение |
|---|---|
| Метрик в отчёте | **19** в 6 разделах: оборона, атака, движение без мяча, игроки, стандарты, ритм |
| Типов данных фида | **11** объявленных `Capability` — от событий и трекинга до цепочек прессинга и xThreat |
| Кодовая база | ~3 100 строк Python, 19 модулей, без внешнего рантайма в отчёте |
| Тесты | **17**, включая независимый пересчёт метрик прямо из сырого CSV |
| Выход | **1 HTML-файл**, открывается с диска, работает без сервера и интернета |
| Подключение новой лиги | **1 адаптер**, логика метрик не меняется |

## Задача

Подготовка к матчу упирается в механику. Видеоаналитик два-три дня выгружает
данные, сводит матчи в одну систему координат, считает метрики, ищет и режет
эпизоды. К совещанию тренер получает презентацию, в которой утверждения нельзя
проверить: чтобы увидеть эпизод за тезисом, нужно снова идти к аналитику. Чем
ближе тур, тем больше в отчёте «мне показалось» и меньше проверяемого.

## Решение

Система забирает всю механику и отдаёт к утру собранный отчёт, в котором каждый
тезис кликается в эпизоды. Аналитик перестаёт работать руками: он проверяет,
спорит, добавляет то, что видит глазом, и несёт тренеру уже своё.

| Этап | Что делает система |
|---|---|
| Сбор | Матчи соперника из фида лиги, сведение к системе координат одной команды |
| Расчёт | 19 метрик; каждая объявляет, что ей нужно от источника |
| Контекст | Сравнение каждого числа с медианой лиги по всей выборке |
| Выводы | Тезисы, ранжированные по весу доказательства, с привязкой к эпизодам |
| Рендер | Самодостаточный HTML: поиск по тактическим паттернам, переход в эпизод в два клика |

## Инженерные решения, на которых держится доверие

- **Честность важнее полноты.** Метрика декларирует требуемые `Capability`,
  источник — фактические. Нет пересечения — блок уходит в «нет данных на этом
  фиде», а не возвращает число, посчитанное не из того.
- **Тезис без эпизодов не выпускается.** Правило закреплено тестом
  `test_every_insight_has_clips`, а не договорённостью в команде.
- **Факт отделён от гипотезы.** Арифметика помечена `ФАКТ`; тактический вывод —
  `ГИПОТЕЗА` в формулировке «в N эпизодах из M это выглядит так, вот все N».
- **Система координат под тестами.** Ошибка знака не роняет расчёт, а тихо
  переворачивает половину выводов, поэтому проверяется инвариантами: вратарь у
  своих ворот, нападающий впереди защитников, левый канал по одну сторону оси.
- **Граница автоматизации проведена в коде.** Что скажет команде тренер —
  не автоматизируется принципиально.

## Стек и запуск

Python 3.11+, pandas, numpy, jinja2. Внешних сервисов нет, данные не покидают контур клуба.

```bash
pip install -r requirements.txt
python -m takt.cli report --team "Auckland FC" --out out/report.html
```

> **Статус.** Ядро и рендер готовы, покрыты тестами. Полный прогон pytest требует
> выгрузку фида в `data/`, поэтому в CI проверяются компиляция и импорты;
> подключение конкретной лиги — отдельный адаптер в `takt/sources/`.

---

## Что внутри

| Слой | Файл | Отвечает за |
|---|---|---|
| Модель | `takt/model.py` | Единое представление матча: события + трекинг, любой поток может отсутствовать |
| Источники | `takt/sources/` | Адаптеры поставщиков данных. Один файл на поставщика |
| Метрики | `takt/metrics/` | Расчёты. Каждая объявляет, что ей нужно от фида |
| Лига | `takt/baseline.py` | Медиана по всем командам выборки — контекст для чисел |
| Тезисы | `takt/insights.py` | Автогенерация выводов с весом и привязкой к эпизодам |
| Отчёт | `takt/report.py` | Сборка модели отчёта + поиск по тактическим паттернам |
| Рендер | `takt/render/` | Самодостаточный HTML без внешних зависимостей |

### Ключевое проектное решение

**Метрика объявляет, что ей нужно от источника.** Источник объявляет, что он
умеет. Если пересечения нет — метрика не считается и попадает в раздел
«нет данных» вместо того, чтобы вернуть число, посчитанное не из того.

```python
@metric("defensive_line", "Высота последней линии", "Оборона",
        requires=(Capability.DEFENSIVE_LINE,))
def defensive_line(ms: MatchSet) -> dict:
    ...
```

Практический смысл: мы не знаем заранее, что отдаёт фид конкретной лиги.
Подключение нового поставщика = новый адаптер + честная декларация
возможностей. Логика метрик при этом не меняется ни на строку, а клуб на
вкладке «Фид и метод» сразу видит, какие блоки отчёта у него появятся.

### Система координат

`x ∈ [−52.5, +52.5]`, `y ∈ [−34, +34]`, метры, начало — центр поля.
Ось x направлена в сторону атаки команды, владеющей мячом.
Сведение всех матчей к системе одной команды — `Match.frame_of(team_id)`.

Это самое опасное место в проекте: ошибка знака не роняет расчёт, а тихо
переворачивает половину выводов. Поэтому на систему координат отдельные
тесты: вратарь обязан оказаться у своих ворот, нападающий — впереди
защитников, левый канал — по одну сторону от центра.

---

## Граница автоматизации

На ней держится доверие штаба, поэтому она проведена в коде, а не в презентации.

**Автоматизируется полностью — всё проверяемое.** Цифры, карты, распределения,
нарезка эпизодов, поиск по паттернам, сравнение с лигой. Здесь система не
ошибается, потому что ошибаться нечему: это арифметика над данными.
В отчёте помечено как `ФАКТ`.

**Автоматизируется как гипотеза — тактические выводы.** Не «они делают X»,
а «в N эпизодах из M это выглядит так — вот все N, посмотри». Формулировка
меняет всё: система не утверждает, она показывает и ранжирует по весу
доказательства. В отчёте помечено как `ГИПОТЕЗА`.

**Не автоматизируется — то, что тренер скажет команде.** Это его работа.

Отсюда жёсткое правило, проверяемое тестом: **тезис без эпизодов не
выпускается**. Если утверждение нельзя проверить за два клика, его нет.

```python
def test_every_insight_has_clips(...):
    for i in insights:
        assert i.clips, f"тезис без эпизодов не должен выпускаться: {i.text}"
```

### Вес тезиса

`вес = 0.65 × сила отклонения от лиги + 0.35 × уверенность от объёма выборки`

Отчёт, где всё важно, не читают. Тезисы сортируются по весу, цветовая
градация — по нему же.

---

## Подключение к фиду лиги

`takt/sources/yandex.py` — рабочая точка подключения с описанным контрактом
и списком вопросов, которые нужно закрыть до начала работ. Порядок вопросов
не случайный: первые два определяют, проект это на три месяца или на полтора года.

1. Отдаётся ли сырой трекинг или только производные события?
2. Есть ли поверх трекинга забегания, цепочки прессинга, взломы линий?
3. Формат и частота выгрузки, глубина истории.
4. Маппинг идентификаторов игроков и команд.
5. Тайм-коды видео в той же системе кадров, что и данные.

Метод `probe()` предназначен для первой же команды после получения доступа:
скачать один матч, разобрать структуру, вернуть список реально пришедших
полей. Функция `capabilities_from_fields()` выводит из него возможности —
чтобы решение «что мы умеем на этом фиде» принималось по данным, а не на словах.

---

## Данные в демонстрации

Открытый датасет **SkillCorner Game Intelligence** (A-League 2024/25, 10 матчей).
Он выбран не случайно: по типу это ровно то, что даёт оптическая система лиги —
камеры → нейросетевой трекинг → производные события. 294 признака на событие,
включая забегания без мяча, цепочки прессинга, взломы линий, варианты передачи,
ширину и длину блока по фазам.

Российских матчей нет ни в одном открытом датасете и не будет. Реальный отчёт
по клубу РПЛ появится только после подключения к фиду лиги.

**Чего в демонстрации нет:** видео. Открытый датасет отдаёт координаты и события,
но не записи матчей, поэтому кнопка эпизода показывает карточку с тайм-кодом
вместо момента игры. В контуре клуба на этом месте открывается запись.

---

## Проверка

```
python -m pytest tests/ -q
```

17 проверок. Кроме обычных инвариантов там продублирован независимый пересчёт
нескольких метрик прямо из сырого CSV: если пайплайн начнёт врать, тест это
поймает. Отдельно проверяется, что урезанный фид не ломает пайплайн, а честно
помечает недоступные метрики.

---

## Установка

```
pip install -r requirements.txt
git clone https://github.com/SkillCorner/opendata.git data/skillcorner
python -m takt.cli matches --team "Auckland FC"
python -m takt.cli report --team "Auckland FC" --out out/report.html
```

Развёртывание в клубе — on-prem, без внешних зависимостей во время работы:
единственное исходящее соединение — запрос к API лиги по расписанию.
